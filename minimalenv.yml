channels:
  - conda-forge
  - defaults
dependencies:
  # Platform agnostic build dependencies
  - c-compiler
  - cxx-compiler
  - fortran-compiler
  - cmake
  - ninja
  - setuptools<60.0.0
  - f90wrap
