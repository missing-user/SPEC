channels:
  - conda-forge
dependencies:channels:
  - conda-forge
dependencies:
  # Platform agnostic build dependencies
  - c-compiler
  - cxx-compiler
  - fortran-compiler

  # Libraries
  - setuptools<60.0.0 # for f90wrap
  - f90wrap
